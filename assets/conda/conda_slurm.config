process {

    withName: FORMAT_KEGG_DB {
        publishDir = [
            path: "${params.kegg_db}",
            mode: 'move',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-format-kegg-db"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: RENAME_FASTA {
        publishDir = [
            path: "${params.outdir}/Processed_inputs",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-rename"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: CALL_GENES {
        publishDir = [
            path: "${params.outdir}/Prodigal_v2.6.3",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-call"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: QUAST {
        publishDir = [
            path: "${params.outdir}/QUAST_v5.2.0",
            mode: 'copy',
        ]

        conda = "${params.quast_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-quast"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: GENE_LOCS {
        publishDir = [
            path: "${params.outdir}/Processed_inputs",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-gene-locs"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: TRNA_SCAN {

        publishDir = [
            path: "${params.outdir}/tRNA_rRNA",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-tRNA-scan"

        maxForks = "${params.max_forks_user_cpu}"
    }

    withName: RRNA_SCAN {
        publishDir = [
            path: "${params.outdir}/tRNA_rRNA",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-rRNA-scan"

        maxForks = "${params.max_forks_user_cpu}"
    }

    withName: TRNA_COLLECT {
        publishDir = [
            path: "${params.outdir}/tRNA_rRNA",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = 1
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-tRNA-collect"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: RRNA_COLLECT {
        publishDir = [
            path: "${params.outdir}/tRNA_rRNA",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = 1
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-rRNA-collect"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: MMSEQS_INDEX {

        publishDir = [
            path: "${params.outdir}/MMSEQS2",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-mmseqs-index"

        maxForks = "${params.max_forks_single_cpu}"
    }   

    withName: MMSEQS_SEARCH {
        publishDir = [
            path: "${params.outdir}/MMSEQS2",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.lrg_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-mmseqs-search"

        maxForks = "${params.max_forks_user_cpu}"
    }  

    withName: ADD_SQL_DESCRIPTIONS {
        publishDir = [
            path: "${params.outdir}/MMSEQS2",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.med_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-add-sql-descriptions" : "--job-name=DRAM-add-sql-descriptions"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: HMM_SEARCH {
        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = '2'
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-search" : "--job-name=DRAM-hmm-search"

        maxForks = "${params.max_forks_hmm}"
    }

    withName: PARSE_HMM {
        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-parse-hmm" : "--job-name=DRAM-parse-hmm"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: KEGG_HMM_FORMATTER {
        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-kegg-format" : "--job-name=DRAM-hmm-kegg-format"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: KOFAM_HMM_FORMATTER {
        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-kofam-format" : "--job-name=DRAM-hmm-kofam-format"

        maxForks = "${params.max_forks_single_cpu}"
    }

    withName: CANTHYD_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-kcanthyd-format" : "--job-name=DRAM-hmm-canthyd-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: VOG_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-vog-format" : "--job-name=DRAM-hmm-vog-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: CAMPER_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-camper-format" : "--job-name=DRAM-hmm-camper-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: SULFUR_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = params.slurm_node ? "--nodelist=${params.slurm_node} --job-name=DRAM-hmm-sulfur-format" : "--job-name=DRAM-hmm-sulfur-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: FEGENIE_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-hmm-fegenie-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: GENERIC_HMM_FORMATTER {
    

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-hmm-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: DBCAN_HMM_FORMATTER {

        publishDir = [
            path: "${params.outdir}/HMM_search",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-hmm-dbcan-format"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: COMBINE_ANNOTATIONS {
        
        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-combine-annotations"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: COUNT_ANNOTATIONS {

        
        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-count-annotations"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: ADD_ANNOTATIONS {

        
        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-add-annotations"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: MERGE_ANNOTATIONS {

        
        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-merge-annotations"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: ADD_TAXA {

        
        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-taxa"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: ADD_BIN_QUALITY {

        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-bin-quality"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: GENERATE_GFF_GENBANK {

        publishDir = [
            path: "${params.outdir}/RAW",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-GFF-GBK"

        maxForks = "${params.single_thread_job}"
    }
    withName: COMBINE_DISTILL {
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-distill-prep"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: DISTILL {

        publishDir = [
            path: "${params.outdir}/DISTILL",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-distill-final"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: PRODUCT_HEATMAP {
    

        publishDir = [
            path: "${params.outdir}",
            mode: 'copy',
        ]
        
        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.single_thread_job}"
        time = "${params.time}"
        memory = "${params.sml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-product-heatmaps"

        maxForks = "${params.max_forks_single_cpu}"
    }
    withName: TREES {
    

        publishDir = [
            path: "${params.outdir}",
            mode: 'copy',
        ]

        conda = "${params.main_environment}"
        executor="slurm"
        cpus = "${params.threads}"
        time = "${params.time}"
        memory = "${params.xsml_job}"
        queue = "${params.slurm_queue}"
        clusterOptions = (params.slurm_node ? "--nodelist=${params.slurm_node} " : "") + "--job-name=DRAM-trees"

        maxForks = "${params.max_forks_single_cpu}"
    }  
}
